<Window x:Class="tabliczka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:tabliczka.ViewModels"
        mc:Ignorable="d"
        Title="Matematyczny Mistrz"
        Height="400"
        Width="600">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Matematyczny Mistrz" FontSize="28" FontWeight="Bold" Margin="0 0 0 20" HorizontalAlignment="Center"/>
            <TextBlock FontSize="24" HorizontalAlignment="Center">
                <Run Text="{Binding CurrentQuestion.Number1}"/>
                <Run Text=" × "/>
                <Run Text="{Binding CurrentQuestion.Number2}"/>
            </TextBlock>
            <TextBox Text="{Binding UserAnswer, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="40" FontSize="20" Margin="0 20"/>
            <Button Content="Sprawdź" Width="200" Height="40" Command="{Binding CheckAnswerCommand}"/>
            <TextBlock Text="{Binding ResultMessage}" FontSize="20" Margin="0 20"  HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Score, StringFormat=Wynik: {0}}" FontSize="18" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>